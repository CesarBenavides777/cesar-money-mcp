# Monarch Money credentials (required)
MONARCH_EMAIL=your-email@example.com
MONARCH_PASSWORD=your-password
MONARCH_MFA_SECRET=your-totp-secret-key

# Server config (optional — defaults shown)
PORT=3200
TRANSPORT=stdio

# OAuth config (required for HTTP/connector mode only)
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=

# CORS allowed origins (comma-separated, for HTTP mode)
CORS_ORIGINS=https://claude.ai,https://www.claude.ai,https://claude.com

# Rate limiting
RATE_LIMIT_RPM=60

# Database
DB_PATH=monarch-mcp.db

# Logging
LOG_LEVEL=info

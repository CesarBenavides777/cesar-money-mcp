# Monarch Money Credentials
MONARCH_EMAIL='bcqyg42yby@privaterelay.appleid.com'
MONARCH_PASSWORD='$5D!9Cx9Dc*z7Yw'

# MFA Secret Key
# You can find this in Monarch Money:
# Settings -> Security -> Enable MFA -> Copy the "Two-factor text code"
MONARCH_MFA_SECRET=mfa_secret

# MCP Server Base URL (for OAuth endpoints and discovery)
# If not set, will auto-detect from request headers
# Example: https://your-app-name.vercel.app
BASE_URL=
